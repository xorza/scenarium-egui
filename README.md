# egui-playground

A small egui sandbox for experimenting with widgets and layout.

## Interaction

- Mouse wheel zooms the graph under the cursor (faster).
- Trackpad scroll pans; pinch or Ctrl/Cmd + scroll zooms (faster).

## Notes for AI

See `NOTES-AI.md` for AI-generated implementation details, project structure, and functionality notes. This file is the canonical place for those details; please avoid adding implementation specifics here and update `NOTES-AI.md` instead.
